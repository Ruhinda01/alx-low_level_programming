#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 * @x: Contains the integer to be checked
 * Return: result
 */
int _abs(int x)
{
	if (x < 0)
	{
		return (-x);
	}
	return (x);
}
