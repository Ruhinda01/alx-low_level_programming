#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: 0 (Success)
 */

int main(void)
{
	int base = 0;

	/* Using the while loop to achieve the result */

	while (base < 10)
	{
		printf("%d", base);
		base++;
	}
	printf("\n");

	return (0);

}
